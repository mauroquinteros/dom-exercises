import hamburgerMenu from './components/hamburger.component.js'

document.addEventListener('DOMContentLoaded', () => {
  // hamburger button
  hamburgerMenu('#hamburger-btn', '#panel' , '.menu-item')
})
